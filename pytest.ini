[pytest]
# Configuration for pytest
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -p no:pytest_socket -p no:socket
markers =
    unit: Simple unit tests without async/network requirements
    enable_socket: Tests that require socket access
